pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // add this
        jcenter()
        maven{
            url 'https://naver.jfrog.io/artifactory/maven/'
        }
    }
}
rootProject.name = "Start1"
include ':app'
